const Card = () => {
  return (
    <div
      style={{
        maxWidth: "768px",
        width: "85vw",
        height: "140px",
        backgroundColor: "#fff",
        borderRadius: "6px",
        padding: "4px",
        boxShadow: "0px 3px 10px 2px rgba(0, 0, 0, 0.4)",
      }}
    >
      Aqui vai ficar o conteúdo
    </div>
  );
};

export default Card;
